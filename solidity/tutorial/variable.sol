pragma solidity 0.8.13; /// You need enter the version

/// Add class
contract test{
    int a = -5; /// Like python int
    uint b = 3; /// Only positive number
    bool c = true; /// Only true and false
    string d = "hello world"; /// Like python str
    bytes10 e = "Only10Word"; /// Only can type 10 word 
    bytes32 f = "Only32Word"; /// bytes have a lot of types
    bytes3 g = "ETH"; /// bytes a lot of time use like this
    address h = "0x95ad61b0a150d79219dcf64e1e6cc01f0b64kjke"; /// address
    /// Solidity dosen't have float type. Because a lot of time like this 0.000134eth
}
